# v0.1 release checklist

## Automated gates

- [x] [Linux CI](https://github.com/Sakuya1998/aws-cost-exporter/actions/runs/29551221431) green: tests, race, 75% coverage, assets, container
- [x] `./test/perf/...` passes (series budget, API budget, scrape baseline)
- [x] `./test/release/...` passes (GoReleaser + release workflow)
- [x] [v0.1.5 release](https://github.com/Sakuya1998/aws-cost-exporter/actions/runs/29551589446) succeeded; draft artifacts and release notes reviewed before publication

## Runtime contracts

- [x] Scrape does not call AWS (`test/perf/gate_test.go`)
- [x] Startup refresh = 8 `GetCostAndUsage` + 1 `GetCostForecast` when unpaginated
- [x] Paginated deployments: perf API-budget gate (8+1) does not apply; verify `pagination_pages_total` and alert thresholds instead
- [x] `max_pages=50` hard-stops paginated queries; `series_limit` caps export only
- [x] `series_limit=1000` conserves totals; 1000-series scrape < 15s
- [x] `/ready` matches cache freshness; currency-safe metrics; no cartesian grouping
- [x] README/IAM/dashboard/rules/chart/config compatibility tests pass

## Supply chain

- [x] v0.1.5 signed GHCR image + Helm OCI chart verified with `cosign verify` ([verification record](v0.1.5-verification.md))
- [x] v0.1.5 Trivy HIGH/CRITICAL scan clean for linux/amd64 and linux/arm64
- [x] Chart `appVersion`/image tag match SemVer; only stable tags update `latest`
- [x] Multi-arch build verified (`test/container` and release workflow)

## Manual sign-off

- [x] MVP IAM policy verified on a real read-only AWS account (maintainer-confirmed 2026-07-17)
- [x] v0.1.5 release notes and artifacts reviewed before publication
- [x] arm64 runtime smoke (`docker run --platform linux/arm64`) passed (maintainer-confirmed 2026-07-17)

See `ROADMAP.md`, `SECURITY.md`, `docs/operations/troubleshooting.md`.
